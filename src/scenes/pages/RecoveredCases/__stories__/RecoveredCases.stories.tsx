import React from 'react';
import {storiesOf} from '@storybook/react-native';

import RecoveredCases from '../RecoveredCases';

storiesOf('Pages|RecoveredCases', module).add('default', () => (
  <RecoveredCases />
));
