import React from 'react';
import {storiesOf} from '@storybook/react-native';

import ConfirmedCases from '../ConfirmedCases';

storiesOf('Pages|ConfirmedCases', module).add('default', () => (
  <ConfirmedCases />
));
