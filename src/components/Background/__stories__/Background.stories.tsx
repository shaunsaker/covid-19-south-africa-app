import React from 'react';
import {storiesOf} from '@storybook/react-native';

import Background from '../Background';

storiesOf('Components|Background', module).add('default', () => <Background />);
